'use strict';

describe('Service: daos/common/project', function () {

  // load the service's module
  beforeEach(module('oxApp'));

  // instantiate service
  var daos/common/project;
  beforeEach(inject(function (_daos/common/project_) {
    daos/common/project = _daos/common/project_;
  }));

  it('should do something', function () {
    expect(!!daos/common/project).toBe(true);
  });

});
